{
  "name": "helm",
  "version": "1.0.0",
  "private": true,
  "description": "Helm — a cross-project ops + observability command center for a 7-project ecosystem.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "syntax": "node --check server/index.js && node --check server/fleet.js && node --check server/probe.js && node --check server/mcp.js && node --check public/app.js && node --check public/theme-init.js",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run syntax && npm test"
  },
  "dependencies": {
    "express": "^4.22.3"
  }
}
